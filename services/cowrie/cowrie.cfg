# Trap House cowrie overrides.
# Cowrie reads its packaged cowrie.cfg.dist first, then this file, so only the
# values that differ from the defaults are listed here.

[honeypot]
# Hostname matches the web app served by deception-gw so the SSH-to-web pivot
# lands on one coherent host (corp-webapp-01).
hostname = corp-webapp-01
# Write all cowrie logs (text and JSON) to the shared trap-logs volume.
log_path = /var/log/trap-house
# Serve custom honeyfs file contents (decoy .env, README) in the fake shell.
# contents_path overrides file content from the pickle for any file that
# exists at this path. The pickle defines the directory tree.
contents_path = /cowrie/cowrie-git/honeyfs
# Load our curated credential database (no stock 'phil' user, no wildcard).
auth_class = UserDB

[shell]
# Custom pickle with /home/admin/ directory and decoy .env + README.md.
# Built by injecting entries into the bundled fs.pickle.
filesystem = /cowrie/cowrie-git/var/fs.custom.pickle
# Present a modern Ubuntu 22.04 kernel, matching the deception-gw webshell's
# uname output. The cowrie default (Debian 3.2.0, 2013) is an obvious tell.
kernel_version = 5.15.0-91-generic
kernel_build_string = #101-Ubuntu SMP Tue Nov 14 13:30:08 UTC 2023
hardware_platform = x86_64
operating_system = GNU/Linux

[ssh]
enabled = true
listen_endpoints = tcp:2222:interface=0.0.0.0
# Override cowrie's default SSH-2.0-OpenSSH_6.0p1 Debian banner, which network
# scanners flag as cowrie. Match a current Ubuntu 22.04 OpenSSH build.
version = SSH-2.0-OpenSSH_8.9p1 Ubuntu-3ubuntu0.10

[output_jsonlog]
# JSON event log consumed by the log-shipper.
enabled = true
logfile = /var/log/trap-house/cowrie.json
epoch_timestamp = false

[telnet]
# Disabled by default in cowrie. Phase 1a maps the telnet port, so enable it.
enabled = true
listen_endpoints = tcp:2223:interface=0.0.0.0