# Connection details for scripts/digest.sh.
# Copy to scripts/digest.env (which is gitignored) and fill in your VPS.
VPS_HOST=your.vps.ip.or.hostname
VPS_USER=your_ssh_user
VPS_PORT=22
# Optional: override where digests are written.
# DIGEST_DIR="${HOME}/projects/trap-house/digests"
